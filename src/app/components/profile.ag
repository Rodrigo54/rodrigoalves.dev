<script lang="ts">
  // @ts-nocheck
  import avatar from '@components/avatar.ag' with { analog: 'imports' };
  import { RouterLink } from '@angular/router' with { analog: 'imports' };

  const userInfo = {
    name: `Rodrigo Alves`,
    position: `Desenvolvedor Web Full Stack`,
    description: `Sou um Full Stack Web Developer que gosta de aprender novas formas de programar. Tento me esforçar para ser um bom artista na web.`,
  };
</script>

<template>
  <a [routerLink]="['/blog']">
    <avatar />
    <h1>{{ userInfo.name }}</h1>
    <h2>{{ userInfo.position }}</h2>
  </a>
  <p>{{ userInfo.description }}</p>
</template>

<style>
  :host {
    display: block;
    text-align: center;
  }
  a {
    color: var(--color1-contrast);
    text-decoration: none;
    transition: color 0.5s;
    &:hover {
      color: var(--color3-light);
    }
    h1 {
      display: block;
      font-family: var(--font-serif);
      font-size: 1.5rem;
      margin-top: 1.5rem;
      font-weight: normal;
    }
    h2 {
      display: block;
      font-family: var(--font-sans-serif);
      font-size: 1rem;
      margin-top: 1rem;
      font-weight: normal;
    }
  }
  p {
    color: var(--color1-contrast);
    margin: 1.6rem 0;
    font-family: var(--font-sans-serif);
    font-size: 0.9rem;
    font-weight: 300;
    line-height: 1.4;
  }
</style>
