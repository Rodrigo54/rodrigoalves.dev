<script lang="ts">
  // @ts-nocheck
  import sidebar from '@components/sidebar.ag' with { analog: 'imports' };
</script>

<template>
  <aside class="sidebar">
    <sidebar />
  </aside>
  <main class="content">
    <ng-content></ng-content>
  </main>
  <aside class="menubar">
    <ng-content select="[slot='menubar']"></ng-content>
  </aside>
</template>

<style>
  :host {
    display: grid;
    grid-template-columns: 20rem 1fr 3.75rem;
    height: 100vh;
    width: 100vw;
    overflow: hidden;
    background: var(--color1);
  }
  .sidebar {
    background: var(--color1);
  }
  .content {
    background: var(--color1-shade);
    overflow: auto;
  }
  .menubar {
    background: var(--color1);
  }
</style>
