<script lang="ts">
  // @ts-nocheck
  import profile from '@components/profile.ag' with { analog: 'imports' };
  import navigation from '@components/navigation.ng' with { analog: 'imports' };
  import socialLinks from '@components/social-links.ag' with { analog: 'imports' };
</script>

<template>
  <profile />
  <social-links />
  <navigation />
</template>

<style>
  :host {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    padding: 2rem;
    @media (max-width: 1170px) {
      align-items: flex-start;
      justify-content: center;
      padding: 1rem;
      navigation, social-links {
        display: none;
        visibility: hidden;
      }
    }
  }
</style>
